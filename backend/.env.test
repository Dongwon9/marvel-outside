# Test environment
# Uses same PostgreSQL but separate schema for isolation
NODE_ENV="test"
DATABASE_URL="postgresql://marvel:marvel_password@localhost:5432/marvel_test"

# Redis Configuration
REDIS_URL="redis://:marvel_redis_password@localhost:6379"

# JWT Configuration
JWT_SECRET="test-secret-key"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="test-refresh-secret-key"
JWT_REFRESH_EXPIRES_IN="7d"
REFRESH_TOKEN_TTL="604800"
